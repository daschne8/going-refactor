<h2>We Need:</h2>
<% if @filter %>
  <%=@filter%><br><br>
<% end %>
<% @goods.each do |good|%>
    <%= "#{good.name} - #{good.occupant.name}" %>
    <%# <%= link_to "Edit", edit_establishment_good_path(good.occupant.establishment,good), method: :get %>
    <%# <%= link_to "Delete", good, method: :delete %><br> %> %>
<% end %>
